---
"@opsydyn/oxlint-effect": minor
---

Add Error Modeling Slice 1 with public Effect error-channel rules for generic `Error`, `unknown`, and mixed primitive error shapes. Expose the `errorModeling` preset and include it in `ddd`.
